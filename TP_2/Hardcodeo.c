
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "prototype.h"
/*/
char names[10][20] = {
    "Juan",
    "Pedro",
    "Sofia",
    "miguel",
    "valentina",
    "Lucas",
    "Camila",
    "Cristina",
    "Nicolas",
    "Javier"
};

char sex[10] = {'m','m','f','m','f','m','f','f','m','m'};

int ages[10] = {19,22,26,19,30,25,32,20,21,24};

float height[10] = {1.90,1.75,1.60,1.63,1.70,1.80,1.60,1.65,1.70,1.83};

int hardcodearPeople(sEmployee list[], int size, int nPeople, int* pId){

int cont = -1;

    if(list != NULL && size > 0 && nPeople <= size && *pId != NULL){
        for (int = 0; i < nPeople; i++){
            list[i].id = *pId;
            (*pId)++;
            strcpy(list[i].nombre, nombres[i]);
            list[i].sex = sex[i];
            list[i].age = age[i];
            list[i].height = height[i];
            list[i].isEmpty = 0;
            nPeople++;
            }

    cont = 0;
}

    return cont;
}
*/
